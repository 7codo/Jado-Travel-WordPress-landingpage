/** Owl carousel settings **/
jQuery(document).ready(function () {
  jQuery('.owl-carousel').owlCarousel({
    items: 1,
    loop: false,
    rewind: true,
    autoplay: true,
    dots: true,
    dotsEach: true,
  })
})
